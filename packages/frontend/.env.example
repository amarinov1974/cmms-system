VITE_API_URL=http://localhost:3000/api

# Optional: Must match backend API_KEY. If set, sent as x-api-key on every request.
# VITE_API_KEY=your-secret-api-key

# Optional: Access code gate for teammates. If set, users must enter this code before seeing the app.
# VITE_ACCESS_CODE=your-team-password
